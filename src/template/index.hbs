<article>
    <!-- TODO: Move the header text so it is structured as the top-most `dt` element of the document. -->
    <dl class="profile" id="http-status-codes">
        {{#each statusCodes}}
            {{> status-code statusCode=statusCodeValue reasonPhrase=reasonPhrase }}
        {{/each}}
    </dl>
</article>
